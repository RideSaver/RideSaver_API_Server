apiVersion: apps/v1
kind: Deployment
metadata:
    name: user-api
    namespace: api
    labels:
        type: service
        service: user
spec:
    selector:
        matchLabels:
            type: service
            service: user
    replicas: 1
    template:
        metadata:
            labels:
                type: service
                service: user
        spec:
            containers:
                - name: user-api
                  image: ghcr.io/ridesaver/user_service:latest
                  ports:
                      - containerPort: 80
                      - containerPort: 443
                - name: user-memcache
                  image: memcached:1.6-alpine
                  ports:
                      - containerPort: 11211
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
    name: user-api
spec:
    scaleTargetRef:
        apiVersion: apps/v1
        kind: Deployment
        name: user-api
    minReplicas: 2 # Maintain at least two for redundancy in case on fails
    maxReplicas: 10
    targetCPUUtilizationPercentage: 50
---
apiVersion: v1
kind: Service
metadata:
    name: user-api
    labels:
        type: service
        service: user
spec:
    ports:
        - port: 80
          name: http
        - port: 443
          name: https
    selector:
        type: service
        service: user
