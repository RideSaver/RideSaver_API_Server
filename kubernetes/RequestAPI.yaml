apiVersion: apps/v1
kind: Deployment
metadata:
    name: request-api
    namespace: api
    labels:
        type: service
        service: request
spec:
    selector:
        matchLabels:
            type: service
            service: request
    replicas: 1
    template:
        metadata:
            labels:
                type: service
                service: request
        spec:
            containers:
                - name: lyft
                  image: ghcr.io/ridesaver/request_service:latest
                  ports:
                      - containerPort: 80
                      - containerPort: 443
---
apiVersion: autoscaling/v1
kind: HorizontalPodAutoscaler
metadata:
    name: request-api
spec:
    scaleTargetRef:
        apiVersion: apps/v1
        kind: Deployment
        name: request-api
    minReplicas: 2 # Maintain at least two for redundancy in case on fails
    maxReplicas: 10
    targetCPUUtilizationPercentage: 50
---
apiVersion: v1
kind: Service
metadata:
    name: request-api
    labels:
        type: service
        service: request
spec:
    ports:
        - port: 80
          name: http
        - port: 443
          name: https
    selector:
        type: service
        service: request
