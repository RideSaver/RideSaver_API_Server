apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "LocationAPI.fullname" . }}-test-connection"
  labels:
    {{- include "LocationAPI.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "LocationAPI.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
