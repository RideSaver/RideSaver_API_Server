apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "EstimateAPI.fullname" . }}-test-connection"
  labels:
    {{- include "EstimateAPI.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "EstimateAPI.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
