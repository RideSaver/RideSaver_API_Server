apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "IdentityService.fullname" . }}-test-connection"
  labels:
    {{- include "IdentityService.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "IdentityService.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
